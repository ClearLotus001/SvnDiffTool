{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["tests/**/*.ts", "tests/**/*.tsx", "shared/**/*.ts"],
  "exclude": ["node_modules", "dist", "dist-electron", "release"]
}
