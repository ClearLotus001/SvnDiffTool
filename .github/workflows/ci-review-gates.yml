name: CI Review Gates

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  review-gates:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Build Rust parser
        run: npm run build:rust

      - name: Workbook regression tests
        run: npm run test:workbook

      - name: Rust format check
        run: cargo fmt --manifest-path rust/Cargo.toml --check

      - name: Rust clippy
        run: cargo clippy --manifest-path rust/Cargo.toml -- -D warnings

      - name: Full build
        run: npm run build

      - name: Build Windows packaging smoke
        run: |
          npx tsx scripts/build-win-installer.ts

      - name: Package size gates
        run: npm run report:package-sizes -- --ci
